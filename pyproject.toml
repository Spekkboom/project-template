[project]
name = "project-template"
version = "0.0.0"
description = "Add your description here"

readme = "README.md"

requires-python = ">=3.13"

dependencies = [
    "pandas>=2.3.2",
    "polars>=1.32.3",
]
